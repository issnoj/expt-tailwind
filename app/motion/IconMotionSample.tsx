import { Board } from '@/components/ui/board';
import { HamburgerMenu } from '@/app/motion/hamburger-menu';
import React from 'react';

export const IconMotionSample = () => {
  return (
    <Board title={'アイコン'}>
      <HamburgerMenu />
    </Board>
  );
};
